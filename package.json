{
  "name": "three-vite-demo",
  "version": "1.0.0",
  "description": "原生 JS + Three.js + Vite 项目",
  "type": "module",
  "scripts": {
    "predev": "node scripts/open-firewall.mjs",
    "dev": "vite --host --port 3000",
    "firewall:setup": "scripts\\firewall-setup.bat",
    "build": "vite build",
    "preview": "vite preview",
    "check:rules": "node scripts/phase1-check.mjs && node scripts/phase2-check.mjs && node scripts/phase3-check.mjs && node scripts/phase4-check.mjs && node scripts/phase5-check.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
